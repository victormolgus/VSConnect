<div class="container content">
  <div class="row justify-content-center">
    <div class="table_config" class="col-lg-6">
      <p style="color: green">
        <%= notice %>
      </p>
      <h1>Friends</h1>
      <table class="table">
        <thead>
          <tr>
            <th scope="col">Nome</th>
            <th scope="col">E-mail</th>
            <th scope="col">Telefone</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @friends.each do |friend| %>
            <% if friend.user==current_user %>
              <tr>
                <%= render friend %>
              </tr>
              <% end %>
                <% end %>
        </tbody>
      </table>  
      <%= link_to "New friend" , new_friend_path, class: "btn btn-primary mb-5" %>
    </div>
  </div>
</div>